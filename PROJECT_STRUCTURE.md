# 📁 Структура проекта Commit to Learn

## Общая структура

```
pcbweb/
├── backend/                 # Backend API (Node.js + Express)
│   ├── src/
│   │   ├── config/          # Конфигурация (база данных)
│   │   ├── middleware/      # Middleware (авторизация)
│   │   ├── routes/          # API маршруты
│   │   ├── utils/           # Утилиты (загрузка файлов)
│   │   └── server.js        # Главный файл сервера
│   ├── prisma/
│   │   ├── schema.prisma    # Схема базы данных
│   │   └── migrations/      # Миграции БД
│   ├── uploads/             # Загруженные файлы
│   ├── Dockerfile
│   ├── package.json
│   └── init-db.sh
│
├── frontend/                # Frontend (React + Material-UI)
│   ├── public/
│   │   ├── logo.png         # Логотип (добавьте свой)
│   │   └── index.html
│   ├── src/
│   │   ├── components/      # React компоненты
│   │   ├── contexts/        # React Context (Auth)
│   │   ├── pages/           # Страницы приложения
│   │   ├── App.js
│   │   └── index.js
│   ├── Dockerfile
│   └── package.json
│
├── docker-compose.yml       # Docker Compose конфигурация
├── README.md               # Основная документация
├── SETUP.md                # Инструкция по настройке
└── .gitignore
```

## Backend API Endpoints

### Авторизация
- `POST /api/auth/register` - Регистрация
- `POST /api/auth/login` - Вход
- `GET /api/auth/me` - Текущий пользователь

### Курсы
- `GET /api/courses` - Список курсов
- `GET /api/courses/:id` - Детали курса
- `POST /api/courses` - Создать курс (преподаватель)
- `POST /api/courses/:id/enroll` - Записаться на курс (студент)

### Материалы
- `GET /api/materials/course/:courseId` - Материалы курса
- `POST /api/materials` - Создать материал (преподаватель)

### Задания
- `GET /api/assignments/course/:courseId` - Задания курса
- `GET /api/assignments/:id` - Детали задания
- `POST /api/assignments` - Создать задание (преподаватель)

### Отправки заданий
- `GET /api/submissions` - Мои отправки (студент) / Все отправки (преподаватель)
- `POST /api/submissions` - Отправить задание (студент)
- `POST /api/submissions/:id/grade` - Оценить задание (преподаватель)
- `POST /api/submissions/:id/comments` - Добавить комментарий

### Прогресс
- `GET /api/progress/course/:courseId` - Прогресс по курсу (студент)
- `POST /api/progress/material/:materialId/complete` - Отметить материал как пройденный

## Frontend Pages

### Публичные
- `/login` - Страница входа
- `/register` - Страница регистрации

### Защищенные (требуют авторизации)
- `/` - Главная страница (Dashboard) - список курсов
- `/course/:id` - Детали курса (материалы и задания)
- `/assignment/:id` - Детали задания (отправка/просмотр)
- `/submissions` - Мои отправленные задания (студент)
- `/teacher` - Панель преподавателя (заглушка)

## Роли пользователей

### STUDENT (Студент)
- Просмотр курсов, на которые записан
- Просмотр материалов курса
- Отправка домашних заданий
- Просмотр оценок и комментариев
- Отслеживание прогресса

### TEACHER (Преподаватель)
- Создание курсов
- Загрузка материалов
- Создание заданий
- Проверка домашних заданий
- Комментирование и оценка работ
- Журнал успеваемости (в разработке)

## База данных

Используется PostgreSQL с Prisma ORM.

Основные модели:
- User - пользователи
- Course - курсы
- Material - учебные материалы
- Assignment - задания
- Submission - отправленные задания
- Grade - оценки
- Comment - комментарии
- Progress - прогресс студентов

## Технологии

### Backend
- Node.js 18+
- Express.js
- Prisma ORM
- PostgreSQL
- JWT для аутентификации
- Multer для загрузки файлов

### Frontend
- React 18
- Material-UI (MUI) 5
- React Router 6
- Axios для HTTP запросов

### Инфраструктура
- Docker & Docker Compose
- PostgreSQL 15


# 🚀 Инструкция по настройке и запуску

## Первый запуск

### 1. Подготовка

Убедитесь, что у вас установлены:
- Docker и Docker Compose
- Git

### 2. Клонирование и запуск

```bash
# Перейдите в директорию проекта
cd pcbweb

# Запустите все сервисы
docker-compose up --build
```

При первом запуске это может занять несколько минут, так как:
- Скачиваются образы Docker
- Устанавливаются зависимости
- Создается база данных PostgreSQL
- Применяются миграции Prisma

### 3. Доступ к приложению

После успешного запуска:
- **Frontend**: http://localhost:3000
- **Backend API**: http://localhost:3001/api
- **Health Check**: http://localhost:3001/api/health

### 4. Создание первого аккаунта

1. Откройте http://localhost:3000
2. Нажмите "Зарегистрироваться"
3. Заполните форму:
   - Имя и Фамилия
   - Email
   - Пароль (минимум 6 символов)
   - Выберите роль: **Студент** или **Преподаватель**

### 5. Добавление логотипа (опционально)

Поместите ваш логотип в файл:
```
frontend/public/logo.png
```

Рекомендуемый размер: 200x80 пикселей (PNG формат)

## Локальная разработка (без Docker)

### Backend

```bash
cd backend

# Установить зависимости
npm install

# Настроить .env файл (скопировать из .env.example)
cp .env.example .env

# Применить миграции
npx prisma migrate dev

# Запустить сервер
npm run dev
```

### Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Запустить dev сервер
npm start
```

## Структура базы данных

База данных автоматически создается при первом запуске:
1. PostgreSQL контейнер создает базу данных `commit_to_learn`
2. Выполняется `init.sql` из `backend/database/` (создание расширений, настройка привилегий)
3. Prisma применяет миграции и создает таблицы

### init.sql

Скрипт `backend/database/init.sql` автоматически выполняется при первом запуске PostgreSQL контейнера и:
- Создает необходимые расширения (uuid-ossp, pg_trgm)
- Настраивает привилегии для пользователя commit_user
- Оптимизирует настройки для разработки

**Важно**: Скрипт выполняется только один раз при создании базы данных. Для пересоздания удалите volume:
```bash
docker-compose down -v
docker-compose up
```

Основные таблицы:
- `users` - пользователи (студенты и преподаватели)
- `courses` - курсы
- `materials` - учебные материалы
- `assignments` - задания
- `submissions` - отправленные задания
- `grades` - оценки
- `comments` - комментарии
- `progress` - прогресс студентов

## Тестирование API

Вы можете использовать Postman или curl для тестирования API:

```bash
# Health check
curl http://localhost:3001/api/health

# Регистрация
curl -X POST http://localhost:3001/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "email": "student@test.com",
    "password": "password123",
    "firstName": "Иван",
    "lastName": "Иванов",
    "role": "STUDENT"
  }'
```

## Остановка приложения

```bash
# Остановить все контейнеры
docker-compose down

# Остановить и удалить volumes (удалит данные БД)
docker-compose down -v
```

## Решение проблем

### Порт уже занят

Если порты 3000 или 3001 заняты, измените их в `docker-compose.yml`:

```yaml
ports:
  - "3002:3000"  # вместо 3000:3000
```

### Ошибки с базой данных

Если возникают проблемы с БД:

```bash
# Остановить и пересоздать volumes
docker-compose down -v
docker-compose up --build
```

### Проблемы с миграциями

```bash
# Войти в контейнер backend
docker exec -it commit-to-learn-backend sh

# Запустить миграции вручную
npx prisma migrate deploy
```

## Дополнительная информация

- Документация Prisma: https://www.prisma.io/docs
- Material-UI: https://mui.com
- React Router: https://reactrouter.com

